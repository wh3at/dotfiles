# Local-only Proton Pass CLI key material loaded by ~/.zshrc
# Generate an encryption key, for example:
# dd if=/dev/urandom bs=1 count=2048 2>/dev/null | sha256sum | awk '{print $1}'
export PROTON_PASS_KEY_PROVIDER=env
export PROTON_PASS_ENCRYPTION_KEY="replace-with-random-key"
